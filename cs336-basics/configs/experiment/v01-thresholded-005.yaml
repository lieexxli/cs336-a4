# @package _global_

paths:
  # Set to your data, tokenized with GPT-2 tokenizer!
  train_bin: /data/c-sniderb/a4-leaderboard/v01-tokenized-thresholded-005.bin
  
  # The validation set which we have already tokenized with GPT-2 tokenizer.
  valid_bin: data/paloma/tokenized_paloma_c4_100_domains_validation.bin
  
  model_output: output/your_data

training:
  wandb_entity: brandon-snider-stanford-university
  wandb_project: cs336-a4
